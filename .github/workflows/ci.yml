name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[dev]'

      - name: Run tests
        run: python -m pytest -q

      - name: Schema fixtures (strict)
        run: |
          python scripts/validate_schema.py --schema schemas/verdict.schema.json \
            --data examples/ai-coding-assistant/verdict.json
          python scripts/validate_schema.py --schema schemas/evidence.schema.json \
            --data examples/ai-coding-assistant/evidence.jsonl

      - name: Deterministic HTML build
        run: |
          python visualization/eduevidence-report/scripts/build_report.py \
            --result examples/ai-coding-assistant/result.json \
            --out /tmp/eduevidence-ci.html
          test -s /tmp/eduevidence-ci.html

      - name: Generated artifacts unchanged (determinism)
        run: |
          # Regenerate each committed artifact with its own generator and diff
          # against the committed version. A difference means the generator or
          # its input moved without a committed regeneration.
          python visualization/eduevidence-report/scripts/build_charts.py \
            --result examples/ai-coding-assistant/result.json --lang zh \
            --out /tmp/chart_specs.ci.json
          python visualization/eduevidence-report/scripts/build_infographics.py \
            --result examples/ai-coding-assistant/result.json --lang zh \
            --out /tmp/infographics.ci.json
          python visualization/eduevidence-report/scripts/build_figures.py \
            --result examples/ai-coding-assistant/result.json --lang zh \
            --out-dir /tmp/figures.ci
          diff -u examples/ai-coding-assistant/chart_specs.json /tmp/chart_specs.ci.json || \
            (echo "ERROR: chart_specs.json is stale — regenerate and commit"; exit 1)
          diff -u examples/ai-coding-assistant/infographics.json /tmp/infographics.ci.json || \
            (echo "ERROR: infographics.json is stale — regenerate and commit"; exit 1)
          diff -u examples/ai-coding-assistant/figures/figure_data.json /tmp/figures.ci/figure_data.json || \
            (echo "ERROR: figures/figure_data.json is stale — regenerate and commit"; exit 1)

      - name: Security scan (fetch private-network guards)
        run: |
          python - <<'EOF'
          import sys
          sys.path.insert(0, '.')
          from integrations.smart_web_fetch import is_private
          assert is_private('http://127.0.0.1:8000') is True
          assert is_private('http://169.254.169.254/latest/meta-data') is True
          assert is_private('https://doi.org/10.1145/3544548.3580919') is False
          print('private-network guard OK')
          EOF
